import { PlayCircle } from 'lucide-react';
import { Placeholder } from '@/screens/Placeholder';

export function PlayerScreen() {
  return <Placeholder title="مشغّل الدورة" back icon={<PlayCircle className="size-7" />} />;
}
